# Handover — e2e integration harness

Date: 2026-07-18  
Branch/worktree: `feat/e2e-integration-harness` / existing feature worktree

## Current state

- Canonical plan/build: `docs/plans/2026-07-17-e2e-integration-harness{,-build}.md`.
- T0 (#94) spike implemented but **not committed**; issue remains open and
  board status is `In Progress`.
- T1 (#95) is the next task and is blocked by #94.
- No plan revision is needed: T0 returned **GO** for its owned seams.

## T0 deliverables

- `test/e2e/spike.mjs` — staged install/trust/provider/tool-loop spike.
- `test/e2e/SPIKE.md` — commands, latest benchmark, and decision table.
- `docs/validation/e2e-integration-harness/t0.json` — PV1 manifest.
- `docs/reviews/task-validate-e2e-integration-harness-t0-2026-07-18/` —
  runner report, validator output, generated agent, manifest copy, and verified
  receipt.
- `package.json` and `package-lock.json` pin
  `@earendil-works/pi-coding-agent` exactly to `0.80.10`.

## Evidence

- `node test/e2e/spike.mjs` — PASS.
- PV2 validator — PASS: `tests.spike`, `static.spike`, `patterns.diff`.
- `verify-task-receipt.mjs --dir docs/reviews/task-validate-e2e-integration-harness-t0-2026-07-18` — PASS.
- `npx biome check test/e2e/spike.mjs`, `node --check`, and `git diff --check` — PASS.

## Next agent

1. Commit the T0 changes using a Conventional Commit.
2. Close #94 and move it to `Done` so the native blocker clears.
3. Claim #95 and move it to `In Progress`.
4. Implement T1 per the Build plan: `harness.mjs`, isolation guards,
   staging/install helpers, transcript/assertion helpers, normalized manifest,
   and the guard self-test.
5. Create T1's PV1 manifest before validation; run its deterministic validator.

Pi 0.80.10 observations: the puppet endpoint must emit OpenAI-compatible SSE;
headless invocations worked with child stdin inherited while stdout/stderr were
captured. Keep `PI_OFFLINE=1`, scratch `HOME`, and the allowlisted environment.
