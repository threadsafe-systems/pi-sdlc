# Architecture Decision Records

One ADR per frozen surface of the `sdlc` skill (the consumer-visible shapes that
evolve additively within a major), plus the name and distribution decisions.
Trigger (all three): hard to reverse, surprising without context, the result of a
real trade-off. Format: context, decision, consequences.
