# ADR 0008: the skill is named `sdlc`

- Context: extracted from `loom-sdlc`; the name must be project-agnostic.
- Decision: name the skill `sdlc` (repo `pi-sdlc`), invoked as `/skill:sdlc`.
- Consequences: the name is part of the discovery contract; renaming later breaks
  invocations and docs.
