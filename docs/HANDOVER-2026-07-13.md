# Handover — pi-sdlc lifecycle-hardening programme

- Date: 2026-07-14 (supersedes this stale handover after merge)
- Branches: `feat/normative-reference-honesty` @ **PR #29** and `feat/skill-relative-path-plumbing` @ **PR #33**; released base `main` @ **v0.5.0** (`730e5ef`)
- Law: this repo dogfoods its own `sdlc` skill. Run `sdlc-status` at session
  start per SKILL.md's startup table and branch on its exit code.

## Current gate

The released base is adopted and ready (`sdlc-status` exits 0). The remaining
adoption-contract honesty work is implemented and awaiting Neil's final PR
sign-off: PR #29 (normative-reference honesty, FS11) followed by PR #33
(skill-relative invocation/path plumbing, FS12, stacked on PR #29).

## Programme state

Governing doc: `docs/plans/2026-07-12-sdlc-lifecycle-hardening.md` (O1–O8).

| Child | State |
|---|---|
| 1. Adoption & contract honesty — sub 1 (Adoption readiness, FS8) | **DONE, v0.4.0** (PR #17) |
| 1 — sub 2 (Adoption bundle + lifecycle checking) | **DONE, v0.5.0** (PR #24) |
| 1 — sub 3 (normative-reference honesty) | **READY FOR SIGN-OFF** (PR #29, FS11) |
| 1 — sub 4 (skill-relative invocation/path plumbing) | **READY FOR SIGN-OFF** (PR #33, FS12) |
| 2. Authoring outcomes & traceability (O3) | not started |
| 3a. Portable validator | DONE, v0.3.0 (PR #16) |
| 3b. Durable state (O5) | not started |
| 4. Model governance (O7) | not started |
| 5. Tracker coherence (O6) | not started |

## What v0.4.0 shipped (child 1 sub 1)

FS8 four-state `sdlc-status`: exits 0 `ready` / 1 `not-adopted` / 2 `error` /
3 `not-ready`; adoption = manifest blob in current `HEAD`; ten canonical
checks; exact text/JSON envelopes; ADR 0015 (supersedes 0010) + ADR 0016
(frozen surface). Non-fatal `inspectRoot`/`inspectConfig`/`inspectModels`
seams in `lib.mjs`. 111 offline tests. Epic #6 + #7–#11 closed, board Done.

Panel record: `docs/reviews/pr-sdlc-adoption-readiness-2026-07-13/` (3 rounds;
2 highs fixed — symlink trust, index-flag smuggling; finding 11, clean-filter
smuggling, dismissed as residual risk, **ratified by Neil at merge**).

## Immediate next task — merge and continue the programme

Neil's remaining action is final sign-off on PR #29 and PR #33. After merge,
release the adoption-contract honesty stream, then begin child 2 (authoring
outcomes and traceability, O3) through its own Brainstorm → Plan →
Specification → Build → Implement → PR gates.

## Mechanics (all committed, working)

- Tracker: board #5 (`PVT_kwDODSkYH84BdK7l`), status field
  `PVTSSF_lADODSkYH84BdK7lzhXuXWo`; options Todo `f75ad846`, In Progress
  `47fc9ee4`, Blocked `2268e054`, In Review `bcf2d092`, Done `98236657`.
  Recipes: `skills/sdlc/assets/tracker-ops.md`.
- Per-task validation: PV1 manifests under `docs/validation/<feature>/`,
  runner `skills/sdlc/scripts/validate-task.sh --report <receipt-dir>/…`,
  receipts modelled on `docs/reviews/task-validate-adoption-readiness-*`
  (manifest.json, runner-report.json, generated-agent.md, validator.md,
  receipt.json → `verify-task-receipt.mjs --dir`).
- Panels: `resolve-panel.sh <phase> --author <vendor> --emit-tasks
  pi-sdlc-<phase>`; dispatch via `subagent` tasks[] with per-task `model`.
  Current task_validate pick: openai-codex/gpt-5.6-terra.

## Gotchas (verified this session)

- `node --test` silently skips missing test files (exit 0) — every PV1
  manifest for a task that ADDS a test file needs a `static.tests-exist`
  (`node --check <file>`) guard.
- A freshly `ensure-panel-agent`-stamped agent is NOT visible to the
  `subagent` tool mid-session; workaround: dispatch `delegate` with the
  generated body verbatim (pr-review/plan/spec agents ARE registered).
- The validator runner rejects `--report` paths outside the repo root.
- git `diff` pathspecs are cwd-relative — always use `:(top)<path>` for
  top-relative paths (see cleanAgainstHead in sdlc-status.mjs).
- Worktrees: create-then-enter via `tool:worktree_session` (implement:before
  hook); `npm ci` inside; dispose refuses ignored files — remove
  `node_modules` and stamped `.pi/agents/pi-sdlc-{pr-review,task-validate}.md`
  first.
- Merge needs owner-authorised `--admin` squash; PR title is the Conventional
  Commit that drives semantic-release.
