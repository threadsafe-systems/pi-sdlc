{
  "schemaVersion": 1,
  "taskId": "t3",
  "buildPlan": "docs/plans/2026-08-08-spec-artifact-skeleton-build.md",
  "repoRoot": ".",
  "ownedScenarios": ["SAS3"],
  "checks": [
    {
      "id": "tests.full",
      "argv": ["npm", "test"],
      "timeoutMs": 300000,
      "scope": ["full"],
      "evidence": ["whole-corpus regression net is green"]
    },
    {
      "id": "tests.task",
      "argv": ["node", "--test", "test/spec-artifact-skeleton.test.js"],
      "timeoutMs": 120000,
      "scope": ["task"],
      "evidence": ["M3 prompt structure (SAS3): eight surfaces A-H in order, B/C/D/F anchors cite the skeleton path, five-component coverage, Delta rounds byte-equal L1, output format byte-equal L2", "M4 reference-never-restate: no canonical rule sentence appears in the prompt"]
    },
    {
      "id": "tests.golden",
      "argv": ["node", "--test", "test/extraction.test.js"],
      "timeoutMs": 120000,
      "evidence": ["golden regen path re-stamps all four phases from the consumer fixture and byte-compares: all four IDENTICAL (zero diff) despite the package prompt edit, because the consumer override resolves first by design"]
    },
    {
      "id": "static.lint",
      "argv": ["npx", "biome", "check", "test/spec-artifact-skeleton.test.js"],
      "timeoutMs": 120000,
      "evidence": ["the task's lintable surfaces are biome-clean (prompt markdown is not a biome target)"]
    }
  ],
  "categories": {
    "tests": {
      "applicability": "required",
      "checkIds": ["tests.full", "tests.task", "tests.golden"]
    },
    "static": {
      "applicability": "required",
      "checkIds": ["static.lint"]
    },
    "scenarios": {
      "applicability": "required",
      "evidence": {
        "SAS3": ["tests.task"]
      }
    },
    "standards": {
      "applicability": "n/a",
      "reason": "no governing standard beyond the declared checks applies to an additive prompt anchor plus contract-test assertions"
    },
    "bannedPatterns": {
      "applicability": "n/a",
      "reason": "no task-diff pattern prohibition beyond lint applies to this task"
    }
  }
}
